[providers]
  [providers.docker]
    endpoint = "unix:///var/run/docker.sock"
    exposedByDefault = true
    swarmMode = false

[api]
  dashboard = true
  debug = true
  insecure = true

 [entryPoints]
  [entryPoints.http]
    address = ":80"